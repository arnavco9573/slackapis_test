export default function AddNewBugReportLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <div className="w-full overflow-hidden  px-9 py-12 mb-0 ">
            {children}
        </div>
    );
}
